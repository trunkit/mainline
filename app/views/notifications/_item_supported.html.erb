<div class="catalog-notification">
  <%= content_tag(:div, image_tag(notification.owner.stream_photo.thumb), class: "photo") %>
  <%= content_tag(:span, "#{notification.owner.name} curated #{notification.subject.name}", class: "message") %>
  <%= notification_time_in_words(notification) %>
</div>
