<!DOCTYPE html>
<html>
	<head>
		<title>Trunkit</title>
		<%= stylesheet_link_tag("application", media: "all") %>
		<%= javascript_include_tag("application") %>
		<%= csrf_meta_tags %>

	</head>

	<body>
		<header>
			<div class="content">
				<a href="/" class="tagline">Trunkit</a>

				<%= main_nav(@hide_main_nav) %>
			</div>
		</header>

		<div id="container">
			<%= yield %>
		</div>

	</body>
</html>
