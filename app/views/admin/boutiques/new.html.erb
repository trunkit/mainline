<%= render("admin/common/error", model: @boutique) if @boutique.errors.present? %>

<div class="panel panel-default">
	<div class="panel-heading">
		<h5>Add New Boutique</h5>
	</div>

	<div class="panel-body">
		<%= render("form") %>
	</div>
</div>
