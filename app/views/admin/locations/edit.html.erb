<%= render("admin/common/error", model: @location) if @location.errors.present? %>

<div class="panel panel-default">
  <div class="panel-heading">
    <%= content_tag(:h5, "Update #{@location.name}") %>
  </div>

  <div class="panel-body">
    <%= render("form") %>
  </div>
</div>
